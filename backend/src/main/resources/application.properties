spring.application.name=TemplateJava6

server.port=8080
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Database — credentials via .env
spring.datasource.url=${DB_URL:jdbc:sqlserver://localhost:1433;databaseName=SUNOVA;encrypt=true;trustServerCertificate=true;sendStringParametersAsUnicode=true;}
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

spring.jpa.hibernate.ddl-auto=update

spring.flyway.validate-on-migrate=false
server.port=8080

>>>>>>> e8cf65ec030ae4afe93e9593ad15147efd37b21b
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=false

app.jwt.secret=${JWT_SECRET:}
app.jwt.expiration-ms=604800000
app.security.enforce-role-checks=true

app.upload.dir=uploads
app.upload.products-dir=uploads/products
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=25MB

app.gemini.api-key=${GEMINI_API_KEY:}

# VNPay — secrets + URL public khi test QR/IPN
payment.vnpay.tmn-code=${VNPAY_TMN_CODE:}
payment.vnpay.hash-secret=${VNPAY_HASH_SECRET:}
payment.vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
payment.vnpay.merchant-api-url=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
payment.vnpay.return-url=${VNPAY_RETURN_URL:http://localhost:8080/api/payments/vnpay/callback}
payment.vnpay.ipn-url=${VNPAY_IPN_URL:http://localhost:8080/api/payments/vnpay/ipn}
payment.vnpay.frontend-return-url=${VNPAY_FRONTEND_RETURN_URL:http://localhost:5173/dat-hang}

online.order.payment-timeout-minutes=15
online.order.timeout-scan-ms=60000

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
app.mail.from-name=SUNOVA
app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:5173}

# GHN — token/shop via .env
ghn.token=${GHN_TOKEN:}
ghn.base-url=https://dev-online-gateway.ghn.vn/shiip/public-api
ghn.shop-id=${GHN_SHOP_ID:}
ghn.service-type-id=2
ghn.from-district-id=${GHN_FROM_DISTRICT_ID:}
ghn.from-ward-code=${GHN_FROM_WARD_CODE:}
ghn.shop-name=SUNOVA Shop
ghn.shop-phone=${GHN_SHOP_PHONE:}
ghn.shop-address=Kho SUNOVA
ghn.default-weight=500
ghn.default-length=20
ghn.default-width=15
ghn.default-height=10
ghn.fallback-fee=30000

spring.config.import=optional:classpath:application-local.properties
